<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>新規ユーザ確認</title>
</head>
<body>
  <h2>ユーザ "<%= member_name %>" は存在しません</h2>
  <form action="/user/<%= userId %>/confirm-add" method="POST">
    <input type="hidden" name="member_name" value="<%= member_name %>">
    <input type="hidden" name="location_id" value="<%= location_id %>">
    <p>新規ユーザとして作成して追加しますか？</p>
    <button type="submit">はい、追加する</button>
    <a href="/user/<%= userId %>">キャンセル</a>
  </form>
</body>
</html>
